#Emacs temp files
*~
auto-save-list*
tramp
eshell*
session*
archives*
elpa*
semanticdb/*
semanticdb*
*.cache
*.data
/projectile-bookmarks.eld
/bookmarks
/recentf
org/*
/smex-items
/local_init.el
.aider*

# Native-compilation output (183 MB, regenerated on demand)
eln-cache/

# Package/tool state, not configuration
transient/
org-roam.db
.org-id-locations
local/
projects

# AI tool state
.claude/
.chatgpt-shell.el

# Vendored upstream clones -- each carries its own .git, so tracking them
# here would create broken gitlinks. Re-clone on a new machine:
#   git clone https://github.com/rhblind/emacs-mcp-server.git site-lisp/emacs-mcp-server
#   git clone https://github.com/AllTheLife/Gemini.el
site-lisp/
Gemini.el/
# Stray root-owned file (22 B, Aug 2024), unreadable as user -- not config
gcc.conf

